name: sudoku_solver
description: A Flutter application for solving Sudoku puzzles using native code.

environment:
  sdk: '^3.10.0'

dependencies:
  flutter:
    sdk: flutter
  # camera: ^0.10.5
  image_picker: ^1.1.2
  ffi: ^2.1.0
  path_provider: ^2.1.1
  path: ^1.8.3

flutter:
  uses-material-design: true
  assets:
    - assets/resnet18_svhn_int8.onnx
